{
  "name": "rtdb-limit-child-nodes-functions",
  "description": "Limit number of child nodes Firebase Functions sample",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run clean && npm run compile",
    "clean": "rimraf lib",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-readme": "firebase ext:info .. --markdown > ../README.md",
    "testIfEmulatorRunning": "wait-on tcp:4001 && jest --passWithNoTests"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@types/express-serve-static-core": "4.17.30",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.10.3",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^4.9.0",
    "jest": "^29.7.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^29.1.2",
    "typescript": "^4.9.4"
  },
  "private": true,
  "devDependencies": {
    "wait-on": "^7.2.0"
  }
}
