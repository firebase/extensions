{
  "name": "export-user-data-functions",
  "description": "Automatically export a user's data when they delete their account.",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run clean && npm run compile",
    "clean": "rimraf lib",
    "compile": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "generate-readme": "firebase ext:info .. --markdown > ../README.md"
  },
  "author": "Jacob Cable",
  "license": "Apache-2.0",
  "dependencies": {
    "csv-stringify": "^6.2.0",
    "firebase-admin": "^9.11.1",
    "firebase-functions": "^3.15.4",
    "firebase-tools": "^9.16.5",
    "rimraf": "^2.6.3",
    "typescript": "^4.4.4"
  },
  "engines": {
    "node": "14"
  },
  "private": true,
  "devDependencies": {
    "@types/jest": "^29.0.3",
    "jest": "^26.6.3",
    "jest-environment-jsdom-fifteen": "^1.0.2",
    "ts-jest": "^24.1.0"
  }
}
