- **BREAKING** feat: add the `DATABASE_REGION` parameter, which places the functions in the Cloud Run region derived from your Firestore database location. Regional locations pass through unchanged and the multi-region locations map to a region inside them (`nam5`/`nam7` to `us-central1`, `eur3` to `europe-west1`) instead of failing the deploy. Requires firebase-tools >= 15.28.0; with an explicit empty `DATABASE_REGION=` line in `.env` the functions declare no region and the Firebase CLI resolves one at deploy time, as before; omitting the line fails a non-interactive deploy. Changing the value on an existing instance deletes and recreates the functions. Upgrading an existing install: add `DATABASE_REGION` to your `.env`, either a value or an explicit empty line, before your next deploy, or the deploy fails on the missing parameter. With `EMBEDDING_PROVIDER=vertex` this also moves the Vertex AI embedding call, which runs in the functions' own region: the embedding functions previously ran in `us-central1`, and five of the offered locations do not serve `gemini-embedding-001`, so check the README before pinning to one of those.
- chore: run on firebase-functions ^7.3.3-rc.1, the same release candidate as the other kits
- The instance id now comes from `FIREBASE_KIT_INSTANCE_ID`, which the Firebase CLI (15.27.0 or later) provides to each kit instance; `INSTANCE_ID` is no longer a configuration parameter
- fix: restore the extension's `Yes` / `No` option labels on the `DO_BACKFILL` and `UPDATE_ON_CONFIGURE` deploy-time prompts. The stored values are unchanged (`true`/`false`), so this is a label-only fix and no `.env` from an earlier deploy needs editing.
- Fixed the backfill and update task dispatch failing with "Queue does not exist": the kit prefixed queue names with `kit-<instance id>-` itself, which the Admin SDK then prefixed again from `FIREBASE_KIT_INSTANCE_ID`. The four `*_QUEUE_NAME` settings now take the deployed function name without that prefix
- OpenAI embeddings are back on the extension's model and size: `EMBEDDING_PROVIDER: openai` requests `text-embedding-ada-002` at its native 1536 dimensions with a batch size of 16, replacing `text-embedding-3-small` pinned at 512 with a batch size of 1. Vectors written by an earlier version of the kit are not comparable with the ones it writes now, so re-embed the collection after upgrading. The vector index the kit creates for OpenAI is still declared with 512 dimensions, exactly as the extension declared it, so it does not cover the 1536-dimension vectors and `findNearest` fails against it; create the 1536-dimension index yourself if you query an OpenAI-embedded collection.
- Initial release of kit, see README for differences between the legacy extension and this kit
- No Eventarc events are published, matching the extension. The extension declares `onStart`, `onSuccess`, `onError` and `onCompletion` under `firebase.extensions.firestore-vector-search.v1.*` in its `extension.yaml` but never publishes any of them. Earlier `0.0.2-rc` builds of this kit published all four from `embedOnWrite` when `EVENTARC_CHANNEL` was set; they no longer do, and `EVENTARC_CHANNEL` is no longer read. If you subscribed to those events on an rc build, the subscription now receives nothing.
- Restored the extension's batched backfill: the backfill and update triggers enumerate the collection by document reference instead of loading it into memory, chunk it into 50 document ids per Cloud Task, run one task at a time, and embed each chunk in provider-sized batches with a single API call per batch. Documents are marked `BACKFILLED` or `FAILED_BACKFILL` in one batched write, and a failed batch no longer fails the task.
- Restored the index metadata gate at `_<instance id>/index`: a backfill or update pass runs only when the embedding provider, the vector dimension or the input/output field names differ from the last recorded pass, so a redeploy that changes none of them no longer re-embeds the whole collection. Unlike the extension, the progress counters are merged into that document rather than replacing it, so the gate survives its own first pass.
- With both `DO_BACKFILL` and `UPDATE_ON_CONFIGURE` set, only the backfill pass is enqueued. The two passes share one task thread on `_<instance id>/index`, and the backfill pass covers every document the update pass would.
- The first kit deploy over an installed extension instance runs a pass, because the extension's own progress writes left no comparison fields on `_<instance id>/index`. The pass follows the extension's rules against the extension's own status field, so it skips documents marked `COMPLETED` or `ERROR` and re-embeds the ones marked `BACKFILLED` or carrying no status. Later deploys are gated as described above. See the README for the full list of backfill differences.
