{
  "name": "invites-mod",
  "version": "0.1.0",
  "description": "Invite people to your app using SendGrid",
  "main": "functions/lib/index.js",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf functions/lib",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Roman Nurik",
  "license": "Apache-2.0",
  "dependencies": {
    "@sendgrid/mail": "^6.3.1",
    "firebase-admin": "^8.0.0",
    "firebase-functions": "^2.3.1"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "typescript": "^3.5.2"
  }
}
